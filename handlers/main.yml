---
- name: Restart rustfs
  ansible.builtin.systemd:
    name: rustfs
    state: restarted
    daemon_reload: true

- name: Reload nginx
  ansible.builtin.systemd:
    name: nginx
    state: reloaded

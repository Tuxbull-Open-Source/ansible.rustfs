---
- name: Install RustFS packages on Debian-family systems
  ansible.builtin.apt:
    name:
      - unzip
      - tar
    state: present
    update_cache: true
  when: rustfs_manage_packages | bool

- name: Mark Debian-family dispatch
  ansible.builtin.set_fact:
    rustfs_dispatch_selected: Debian
